import 'package:flutter/material.dart';

class ListBrandCar extends StatelessWidget {
  const ListBrandCar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(
        color: Colors.red,
      ),
    );
  }
}
